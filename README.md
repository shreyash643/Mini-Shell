# Mini-Shell
A simple shell program in C that performs basic shell tasks including built-in commands like cd, pwd, echo, and exit. It also supports external commands, piping between commands, and job control using fg and bg. 
